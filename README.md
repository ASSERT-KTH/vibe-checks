# ASSERT Vibe Checks

vibe checks with automation and traceability

Run `python3 vibe-check-model.py <model>` for OpenRouter models, or prefix Hugging Face models with `huggingface/`, for example `python3 vibe-check-model.py huggingface/zai-org/GLM-5.2 --no-git`.
